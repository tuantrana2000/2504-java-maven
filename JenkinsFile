pipeline{
    agent any
    tools {
        maven "Maven3"
        jdk "jdk21"
    }
    environment {
      IMAGE_NAME='ducnt65/2504-repo'
      IMG_TAG="tag-${env.BUILD_NUMBER}"
    }
    
    stages {
        stage("Checkout") {
            steps{
                git 'https://github.com/nguyentduc89/2504-java-maven.git'
                script {
                    withCredentials([usernamePassword(credentialsId: 'git-cred', usernameVariable: 'GIT_USER', passwordVariable: 'GIT_TOKEN')]){
                    sh '''
                        sed -i "s#image: .*#image: ducnt65/2504-repo:${IMG_TAG}#" deployment.yaml
                        git config --global user.email "nguyentduc89@gmail.com"
                        git config --global user.name "Trung Duc"
                        git add deployment.yaml
                        git commit -m "update tag to ${IMG_TAG}"
                        git remote set-url origin https://${GIT_USER}:${GIT_TOKEN}@github.com/nguyentduc89/2504-java-maven.git
                        git push  origin HEAD:master
                       ''' 
                    }
                }
            }
        }
        stage("Buld code to jar"){
            steps {
                sh 'java -version'
                sh 'mvn -version'
                sh 'mvn clean package -DskipTests'
            }
        }
        stage ("Build Image"){
            steps {
                script {
                    def img = docker.build("${IMAGE_NAME}:${IMG_TAG}")
                    docker.withRegistry("", "dockerhub-ducnt65"){
                        img.push()
                    }
                }
            }
        }
    }
}
